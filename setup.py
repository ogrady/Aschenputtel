#!/usr/bin/env python

from setuptools import setup

setup(name='Aschenputtel',
      version='0.1',
      description='Bot to count occurrences of emojis.',
      url='',
      author='Len',
      author_email='',
      license='MIT',
      # packages=['aschenputtel'],
      install_requires=[
          'discord',
          'requests'
      ],
      zip_safe=False)
